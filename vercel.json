{
  "builds": [
    {
      "src": "app/app.py",  // Replace with the entry point of your Flask app
      "use": "@vercel/python"  // Use the Python builder
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/app.py"
    }
  ]
}
